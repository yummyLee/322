# 项目协作约定

## Godot 场景必须可直接编辑

- 场景中的物体必须保存为 `.tscn` 中的实际节点，打开场景即可在 Godot 编辑器中查看、选择和编辑。
- 不要仅通过运行时脚本生成场景或物体，也不要让用户必须运行项目才能看到和调整场景布局。
- 模型、地形、装饰、灯光、相机等需要调整的场景内容，优先直接配置在 `.tscn` 中；通过有意义的节点命名和分组，方便整体或单独编辑。
- 可以使用一次性脚本辅助制作，但必须将生成结果保存到 `.tscn`，最终运行时加载已保存的场景，不得重新生成并覆盖用户在编辑器中的修改。
- 脚本用于行为、交互和必要的运行时逻辑，不代替可编辑的场景搭建。确需动态生成物体时，应遵循用户对该功能的明确要求。
- 修改场景后，确认编辑器内可编辑，并验证运行时使用的是保存后的场景内容。

## 可复用场景制作流程

- 根据参考图制作 3D 像素场景时，使用新的个人 skill：`C:/Users/Administrator/.codex/skills/godot-reference-to-pixel3d/SKILL.md`，不使用旧的 `godot-editable-pixel-scene`。先读精简说明，只有修改工具时才读取完整辅助脚本，避免重复消耗上下文。
- 当前窑村的编辑入口为 `village/yao_village.tscn`，运行入口为 `village/main.tscn`。日常修改直接编辑已保存场景，不重跑 `village/tools/build_village.gd`。

## 场景生成的地形细节限制

- 禁止用大面积纯色平面、长方形地块、单个扇形多边形或重复的小多边形堆砌来代表区域地面、道路、坡面和区域过渡。
- 地面必须保存为有真实高度变化的细分网格：相邻细分单元要有不规则边界、轻微起伏和连续的材质过渡，不能只依靠颜色噪声伪装地形。
- 区域之间必须有可读的入口、出口、坡脚、路肩或台阶过渡；不同高度的地面必须通过连续坡面或分段自然台地连接，不能让地块直接相交或悬空拼接。
- 道路必须有宽度变化、边界、磨损和与周围地面的过渡，至少加入符合环境的碎石、脚印、车辙、裂缝、湿干变化或其他使用痕迹；道路边缘可用低矮石缘，但不能用长条矩形替代道路建模。
- 生成后必须检查最终 GPU 画面和移动/旋转观察，清除会闪烁的共面面片、重叠地块和长直矩形；确认 `.tscn` 中保存的是可编辑的细分节点，而不是运行时临时生成结果。

## 追加场景生成硬限制（2026-09-19）

- 以当前玩家可见身高 `H` 为尺度，地形、坡面、道路细分单元的最长边不得超过 `0.5H`；禁止用更大的方块、长板或矩形地块拼接场景。
- 禁止重复平铺同尺寸、同朝向、同材质的地块或道路片段；曲线路径必须使用至少 3 个控制点并平滑重采样，不能用闪电状折线冒充自然道路。
- 箱子、石柱、棺材、火把、推车等重要物体必须是独立可编辑资产组/子场景，包含主体、连接件、磨损和接地细节，再以实例放入区域。
- 材质颜色使用共享低饱和调色范围并连续过渡，禁止拼接处突然跳色；生成后必须从俯视、正面、侧面、旋转和玩家同框检查重叠、闪烁、悬空和封闭入口。
- 具体执行清单见 `docs/scenes/乱葬岭左山洞场景建模规范.md`。
